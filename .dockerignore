# Rust build artifacts (we need Cargo.toml and src/ for building)
target/
**/target/
**/*.rs.bk

# Git files
.git/
.gitignore
.gitattributes
.github/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
*.bak
.vs/

# OS files
.DS_Store
Thumbs.db
desktop.ini
*.tmp

# Log files (exclude logs directory to avoid copying log files into image)
*.log
logs/
log/

# Docker files (no need to copy these into the image)
compose.yaml
docker-compose.yml
docker-compose.*.yml
Dockerfile*
.dockerignore

# Documentation (not needed in runtime image)
README.md
LICENSE
*.md
docs/

# Test and development files
tests/
benches/
examples/
.cargo/
rustfmt.toml
.rustfmt.toml

# Environment and local config overrides
.env
.env.*
config/*.local.toml
config/blacklist.json

# CI/CD files
.travis.yml
.gitlab-ci.yml
azure-pipelines.yml
.circleci/

# Coverage and profiling
coverage/
*.profraw
*.profdata
tarpaulin-report.html

# Temporary and backup files
tmp/
temp/
*.backup
*~
